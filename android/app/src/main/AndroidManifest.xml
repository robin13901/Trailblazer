<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ==== Trailblazer permissions (Phase 1 scaffolding) ==== -->

    <!-- Internet — required for glyph/sprite fetches (Protomaps CDN) and
         loopback tile server HTTP requests on Android -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Background location — separate runtime prompt on Android 10+ -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground service (base + type-specific for Android 14+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Activity recognition — Android 10+ (API 29+) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Bluetooth Classic (legacy devices; capped at API 30) -->
    <uses-permission android:name="android.permission.BLUETOOTH"
                     android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
                     android:maxSdkVersion="30" />

    <!-- Bluetooth modern (Android 12+ / API 31+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Notifications — Android 13+ (API 33+); needed for FGS notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Wake lock — required by flutter_background_geolocation in Phase 3 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- ==== end Trailblazer permissions ==== -->

    <application
        android:label="Trailblazer"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Plan 04-18 Task 1 (2026-07-08): Suppress FGB LICENSE VALIDATION FAILURE
             toast on cold start. FGB's native FGS registration checks for the KEY,
             not the value — an empty string is enough to silence the warning without
             paying for a real Android release license. This replaces Plan 04-16-1's
             `bg.Config.reset: true` approach (Option A), which was native-Java-side
             ineffective AND broke trip recording (reset drops persisted trip state
             that manual-start relies on). Option B (this meta-data) is the correct
             fix per FGB docs. -->
        <meta-data
            android:name="com.transistorsoft.locationmanager.license"
            android:value="" />

        <!-- No app-level <service> declaration needed for flutter_background_geolocation.
             FGB merges its own service classes (LocationRequestService and adapters)
             via Android manifest merge. A user-side service declaration would cause
             AAPT duplicate-service errors at build time (see RESEARCH.md Pitfall 2).
             The Phase-1 placeholder .LocationRecordingService has been removed. -->
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Plan 04-18 Task 5 (2026-07-08): Android 11+ package-visibility
             gate. Without these <intent> filters, url_launcher's
             canLaunchUrl() returns false for https:// URLs even when
             Chrome / another browser is installed. Settings > About
             taps on MapTiler / OSM / MapLibre links appeared to no-op
             on the 2026-07-08 drive because of this. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>
</manifest>
