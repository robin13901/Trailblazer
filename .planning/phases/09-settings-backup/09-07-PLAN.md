---
phase: 09-settings-backup
plan: 07
type: execute
wave: 4
depends_on: ["09-03", "09-04", "09-05", "09-06"]
files_modified:
  - lib/features/settings/presentation/settings_screen.dart
  - lib/core/routing/app_router.dart
  - lib/features/settings/presentation/widgets/about_section.dart
  - test/features/settings/presentation/settings_screen_test.dart
autonomous: true

must_haves:
  truths:
    - "The single grouped Settings screen shows Data & Backup, Coverage, Permissions, Diagnostics, and About sections"
    - "The 'Full settings arrive in Phase 10' placeholder tile is gone"
    - "The diagnostics HUD tile + route are reachable in release when the AppPrefs toggle is ON (not kDebugMode-gated); the stress-coverage tile stays debug-only"
    - "The About section shows app version, OSS licenses, and OSM/MapTiler credits"
    - "The coverage-color picker (SET-04) remains reachable"
  artifacts:
    - path: "lib/features/settings/presentation/settings_screen.dart"
      provides: "assembled grouped screen with all sections + toggle-gated diagnostics tile"
      min_lines: 90
    - path: "lib/features/settings/presentation/widgets/about_section.dart"
      provides: "app version + showLicensePage entry added"
      contains: "showLicensePage"
    - path: "lib/core/routing/app_router.dart"
      provides: "/settings/diagnostics reachable in release (gate removed); stress route stays debug-only"
  key_links:
    - from: "settings_screen.dart"
      to: "DataBackupSection / PermissionsSection / RawGpsRetentionSection / CoverageColorSection / AboutSection"
      via: "grouped ListView sections"
      pattern: "(DataBackupSection|PermissionsSection|RawGpsRetentionSection)"
    - from: "settings_screen.dart diagnostics tile"
      to: "AppPrefs.getShowDiagnosticsHud"
      via: "toggle-gated visibility + /settings/diagnostics push"
      pattern: "getShowDiagnosticsHud"
---

<objective>
The serial tail: fold every Phase 9 section into the single grouped Settings screen, replace the "Full settings arrive in Phase 10" placeholder, add a toggle for the diagnostics HUD (reachable in release when ON), keep the stress tile debug-only, un-gate the diagnostics route, and close the SET-09 gap (app version + OSS licenses) in the About section.

This plan is the SOLE OWNER of `settings_screen.dart`, `app_router.dart`, and `about_section.dart` — no other Phase 9 plan writes these, so it runs last with no parallel writers.

Purpose: Satisfies SC5 (About shows version + OSS licenses + OSM credits; HUD toggle present) and makes SET-03/04/05/06/07/08/09 all reachable in one screen.
Output: assembled screen + router un-gate + About completion + a screen widget test.
</objective>

<execution_context>
Ralph loop: `flutter analyze` after every change; run the screen widget test in-loop.
</execution_context>

<context>
@.planning/phases/09-settings-backup/09-CONTEXT.md
@lib/features/settings/presentation/settings_screen.dart
@lib/core/routing/app_router.dart
@lib/features/settings/presentation/widgets/about_section.dart
@lib/features/settings/presentation/widgets/coverage_color_section.dart

Depends on: DataBackupSection (09-05), PermissionsSection (09-04), RawGpsRetentionSection + kShowDiagnosticsHud getter (09-03), HUD extension + un-gated screen (09-06). CoverageColorSection + DataManagementSection already exist. `showLicensePage` + `PackageInfo`-free version: use a `const kAppVersion` sourced from pubspec `version: 0.1.0+1` (hardcode the string; no new dep) OR read via the existing Flutter `showAboutDialog`/`LicensePage` which accepts applicationVersion — hardcode the version string to avoid adding package_info_plus.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete AboutSection (SET-09 gap — version + OSS licenses)</name>
  <files>lib/features/settings/presentation/widgets/about_section.dart</files>
  <action>
The existing AboutSection has MapTiler/OSM/MapLibre credits but is missing app version + OSS licenses (SET-09 requires both). Add, above or below the credits:
- App version row: `Text('Version <kAppVersion>')` where `const kAppVersion = '0.1.0'` (match pubspec `version: 0.1.0+1`; keep the const local to this file with a comment to bump alongside pubspec). No new dependency.
- OSS licenses entry: a tappable row "Open-source licenses" that calls `showLicensePage(context: context, applicationName: 'Trailblazer', applicationVersion: kAppVersion)` (Flutter built-in — aggregates all package LICENSE files). Keep it a ListTile-style tappable with a chevron; the existing section is a Column of Text/Padding, so wrap the new tappable appropriately (InkWell or a ListTile) while preserving the existing credit links + Semantics.
Do NOT remove any existing attribution — the MapTiler/OSM links are a TOS contract.
  </action>
  <verify>`flutter analyze` clean.</verify>
  <done>About shows version, an OSS-licenses entry opening the license page, and retains all credits.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble the grouped Settings screen</name>
  <files>lib/features/settings/presentation/settings_screen.dart</files>
  <action>
Restructure the grouped ListView into the CONTEXT-locked section order: Data & Backup, Coverage, Permissions, Diagnostics, About. Convert `SettingsScreen` to a `ConsumerStatefulWidget`/`ConsumerWidget` as needed (it currently is StatelessWidget; the diagnostics toggle + section reads need `ref`).

Sections:
- **Data & Backup:** `_SectionHeader('Data & Backup')` + `DataBackupSection()` (09-05) + keep the existing `DataManagementSection()` (Refresh admin regions) + `RawGpsRetentionSection()` (09-03).
- **Coverage:** `_SectionHeader('Coverage')` + `CoverageColorSection()` (SET-04, keep reachable).
- **Permissions:** `_SectionHeader('Permissions')` + `PermissionsSection()` (09-04).
- **Diagnostics:** `_SectionHeader('Diagnostics')` + a HUD-visibility SwitchListTile bound to `AppPrefs.getShowDiagnosticsHud`/`setShowDiagnosticsHud` (09-03 keys) + a "Tracking diagnostics" ListTile that pushes `/settings/diagnostics` — shown when the toggle is ON (release-safe). Load the toggle state (FutureBuilder or initState read + setState). Update the diagnostics tile to no longer live behind `if (kDebugMode)`.
- **About:** `_SectionHeader('About')` + `AboutSection()`.
- **Developer (debug-only):** keep `if (kDebugMode) ... _StressCoverageTile()` — the stress harness stays debug-only (RESEARCH risk note: separate the stress-tile gate from the diagnostics-tile gate). The `_DiagnosticsTile` moves into the Diagnostics section under the toggle; `_StressCoverageTile` stays under a `kDebugMode` Developer header.

DELETE the "Coming later / Full settings arrive in Phase 10" placeholder ListTile entirely.
Keep `_SectionHeader`. Use `withValues(alpha:)` for any alpha.
  </action>
  <verify>`flutter analyze` clean.</verify>
  <done>Five sections in CONTEXT order; placeholder gone; diagnostics tile toggle-gated (release-safe); stress tile still debug-only.</done>
</task>

<task type="auto">
  <name>Task 3: Un-gate the diagnostics route (keep stress route debug-only)</name>
  <files>lib/core/routing/app_router.dart</files>
  <action>
Currently `/settings/diagnostics` is registered inside `if (kDebugMode)`. Remove that guard so the route is always registered (it is now reachable via the release toggle). LEAVE `/settings/stress-coverage` inside its `if (kDebugMode)` block (debug-only). Keep the `NoTransitionPage` / builder style consistent with neighbours. Ensure the `TrackingDiagnosticsScreen` import stays (no longer conditionally referenced) and `foundation.dart`/`kDebugMode` is still imported for the stress route.
  </action>
  <verify>`flutter analyze` clean.</verify>
  <done>Diagnostics route always registered; stress route remains debug-only; no unused-import warnings.</done>
</task>

<task type="auto">
  <name>Task 4: Settings screen widget test</name>
  <files>test/features/settings/presentation/settings_screen_test.dart</files>
  <action>
Pump `SettingsScreen` in a `ProviderScope` overriding the providers its sections need (appPrefsProvider with InMemorySharedPreferencesAsync; backupServiceProvider -> FakeBackupService; filePlatformProvider -> FakeFilePlatform; permissionServiceProvider -> FakePermissionService; appDatabaseProvider -> `AppDatabase(NativeDatabase.memory())`; plus coverage preset provider if it reads DB). `pumpAndSettle`. Assert:
- Section headers present: 'Data & Backup', 'Coverage', 'Permissions', 'Diagnostics', 'About'.
- The old 'Full settings arrive in Phase 10' text is ABSENT.
- 'Back up my data' + 'Restore from backup' tiles present (09-05).
- 'Open-source licenses' present (SET-09).
- Diagnostics HUD toggle (SwitchListTile) present.
Keep the override list minimal — if a provider throws when unoverridden, add it. This test is the integration proof that all sections wire together.
  </action>
  <verify>`flutter test test/features/settings/presentation/settings_screen_test.dart` green. `flutter analyze` clean.</verify>
  <done>All sections render together; placeholder gone; backup + license + toggle present.</done>
</task>

</tasks>

<verification>
- `flutter analyze` clean.
- Screen widget test green.
- SET-03/04/05/06/07/08/09 all reachable from this single screen.
- Maps to SC5 (About version + OSS licenses + credits; HUD toggle).

## Deferred on-device checklist (carried to phase close-out / next drive)
Batched with 09-05's device checklist:
- [ ] Diagnostics HUD reachable in a RELEASE build when the toggle is ON (tree-shaking removed the kDebugMode gate correctly).
- [ ] Permissions inspector re-reads after toggling a permission in system Settings and returning to the app.
- [ ] OSS license page renders the aggregated package licenses on-device.
</verification>

<success_criteria>
- Single grouped screen: Data & Backup, Coverage, Permissions, Diagnostics, About.
- Placeholder removed; diagnostics toggle-gated + release-safe; stress tile debug-only.
- About: version + OSS licenses + OSM/MapTiler credits.
</success_criteria>

<output>
After completion, create `.planning/phases/09-settings-backup/09-07-SUMMARY.md`.
</output>
