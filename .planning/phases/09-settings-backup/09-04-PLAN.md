---
phase: 09-settings-backup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/settings/presentation/widgets/permissions_section.dart
  - test/features/settings/presentation/permissions_section_test.dart
autonomous: true

must_haves:
  truths:
    - "User sees a read-only status row for each of: Location Always, Location whenInUse, Motion/Activity, Notifications, Battery optimization"
    - "Each row shows a colored state reflecting the live PermissionService status"
    - "Statuses re-read automatically when the app returns to the foreground (resumed)"
    - "No request prompts or deep-links are triggered (read-only v1)"
  artifacts:
    - path: "lib/features/settings/presentation/widgets/permissions_section.dart"
      provides: "read-only permission status list, re-read on resume"
      min_lines: 70
    - path: "test/features/settings/presentation/permissions_section_test.dart"
      provides: "widget test with FakePermissionService"
  key_links:
    - from: "permissions_section.dart"
      to: "PermissionService.statusAlways/statusWhenInUse/statusActivityRecognition/statusNotification/statusIgnoreBatteryOptimizations"
      via: "read-only status reads on init + resume"
      pattern: "status(Always|WhenInUse|ActivityRecognition|Notification|IgnoreBatteryOptimizations)"
---

<objective>
Deliver SET-03: a read-only permissions inspector section listing five permission rungs with colored states, re-reading on `AppLifecycleState.resumed`, reusing the existing `PermissionService`. No deep-links, no request prompts (v1).

Purpose: Satisfies part of SC4 (permissions inspector shows live status).
Output: `PermissionsSection` widget + widget test using the existing `FakePermissionService`.
</objective>

<execution_context>
Ralph loop: `flutter analyze` after every change; run the widget test in-loop (behavior around lifecycle re-read is worth verifying).
</execution_context>

<context>
@.planning/phases/09-settings-backup/09-RESEARCH.md
@lib/features/onboarding/data/permission_service.dart
@lib/features/settings/presentation/tracking_diagnostics_screen.dart

Reuse: `permissionServiceProvider` (imported in tracking_diagnostics_screen.dart). The HUD's `_refreshAsync` + `_safeStatus` show the exact read-all-five pattern to copy. `FakePermissionService` lives at `test/features/onboarding/fakes/fake_permission_service.dart`. Lifecycle-resume re-read pattern: Plan 03-05 denial banner + app.dart `didChangeAppLifecycleState`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: PermissionsSection widget (read-only, resume-aware)</name>
  <files>lib/features/settings/presentation/widgets/permissions_section.dart</files>
  <action>
`PermissionsSection extends ConsumerStatefulWidget` with `WidgetsBindingObserver`. In `initState`: `WidgetsBinding.instance.addObserver(this)` + kick a `_refresh()`. In `dispose`: `removeObserver`. Override `didChangeAppLifecycleState`: if `resumed`, call `_refresh()`.

`_refresh()` reads all five via `permissionServiceProvider`, each wrapped in a safe try/catch returning null on throw (copy `_safeStatus` from the HUD screen):
- Location Always -> `statusAlways()`
- Location whenInUse -> `statusWhenInUse()`
- Motion/Activity -> `statusActivityRecognition()`
- Notifications -> `statusNotification()`
- Battery optimization -> `statusIgnoreBatteryOptimizations()`
Store in state; `setState` after (guard `mounted`).

Render one ListTile per rung: title = human label, trailing = a small colored dot + status name. Color mapping: granted/limited -> green, denied -> theme error, permanentlyDenied/restricted -> amber/outline, null/loading -> outline "—". Use `withValues(alpha:)` for any dot alpha. Read-only: NO onTap, NO `openAppSettings`, NO request calls.
  </action>
  <verify>`flutter analyze` clean.</verify>
  <done>Five rungs render with colored states; re-reads on resume; zero request/deep-link calls.</done>
</task>

<task type="auto">
  <name>Task 2: Widget test with FakePermissionService</name>
  <files>test/features/settings/presentation/permissions_section_test.dart</files>
  <action>
Pump `PermissionsSection` inside a `ProviderScope(overrides: [permissionServiceProvider.overrideWithValue(fake)])` + MaterialApp. Configure the `FakePermissionService` to return a mix (e.g. Always=granted, whenInUse=granted, activity=denied, notification=granted, battery=permanentlyDenied). `await tester.pumpAndSettle()`. Assert: all five labels present; the denied/permanentlyDenied rows show their status text. If `FakePermissionService` lacks per-status configurability, extend it minimally (it already backs the HUD, so status getters exist) — prefer configuring existing fields over rewriting.
  </action>
  <verify>`flutter test test/features/settings/presentation/permissions_section_test.dart` green. `flutter analyze` clean.</verify>
  <done>Test verifies five rungs render with the injected statuses.</done>
</task>

</tasks>

<verification>
- `flutter analyze` clean.
- Widget test green.
- Maps to SC4 (permissions inspector live status).
</verification>

<success_criteria>
- Five read-only rungs with colored live states.
- Auto re-read on foreground resume.
- No prompts, no deep-links.
</success_criteria>

<output>
After completion, create `.planning/phases/09-settings-backup/09-04-SUMMARY.md`.
</output>
