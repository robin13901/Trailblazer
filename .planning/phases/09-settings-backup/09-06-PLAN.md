---
phase: 09-settings-backup
plan: 06
type: execute
wave: 2
depends_on: ["09-03"]
files_modified:
  - lib/features/matching/data/overpass_way_candidate_source.dart
  - lib/features/settings/data/diagnostics_metrics_provider.dart
  - lib/features/settings/presentation/tracking_diagnostics_screen.dart
  - test/features/matching/data/overpass_cache_counter_test.dart
autonomous: true

must_haves:
  truths:
    - "The diagnostics HUD is reachable in release builds when the AppPrefs toggle is ON (no longer kDebugMode-gated)"
    - "The HUD shows matcher queue depth (pending road-fetch count) live"
    - "The HUD shows an Overpass cache hit-rate metric (or a documented null when no calls yet)"
    - "OverpassWayCandidateSource counts cache hits vs misses on its main-isolate cache-first path"
  artifacts:
    - path: "lib/features/matching/data/overpass_way_candidate_source.dart"
      provides: "in-memory cacheHits/cacheMisses counters + getter"
      contains: "cacheHits"
    - path: "lib/features/settings/data/diagnostics_metrics_provider.dart"
      provides: "provider exposing matcher queue depth + overpass cache hit/miss"
    - path: "lib/features/settings/presentation/tracking_diagnostics_screen.dart"
      provides: "queue-depth + cache-hit rows; kDebugMode gate removed"
  key_links:
    - from: "tracking_diagnostics_screen.dart"
      to: "PendingRoadFetchesDao.listPending().length"
      via: "diagnosticsMetricsProvider"
      pattern: "listPending"
    - from: "tracking_diagnostics_screen.dart"
      to: "OverpassWayCandidateSource.cacheHits/cacheMisses"
      via: "diagnosticsMetricsProvider"
      pattern: "cacheHit"
---

<objective>
Deliver SET-06: make the diagnostics HUD toggleable in release (via the `kShowDiagnosticsHud` AppPrefs key from Plan 09-03), and extend it with matcher queue depth + Overpass cache-hit rate.

**Cache-hit-rate decision (resolves RESEARCH Open Question #4):** adopt Option 1 — add lightweight in-memory `cacheHits`/`cacheMisses` counters to `OverpassWayCandidateSource`, incremented in the tile cache-first path. DOCUMENTED LIMITATION: only the main-isolate cache-first pre-fetch calls are counted; the matcher isolate holds its own source copy whose counters are not surfaced. The HUD shows `—` until the first fetch. This ships IN this wave (not deferred to v-null).

Purpose: Satisfies SC5 (HUD toggle exposes fix rate + matcher queue depth + cache-hit rate).
Output: source counters + a metrics provider + HUD extension + kDebugMode-gate removal + a counter unit test.

NOTE: this plan does NOT touch `settings_screen.dart` (the tile relocation + stress-tile-gate separation is the serial tail, Plan 09-07) and does NOT touch `app_prefs.dart` (owned by Plan 09-03). It only owns the HUD SCREEN, the matching source, and a new provider.
</objective>

<execution_context>
Ralph loop: `flutter analyze` after every change; run the counter test in-loop. Removing the `_ReleaseModeShortCircuit`/`kDebugMode` build guard is behavior-sensitive for release — verify the screen still builds when `kDebugMode` is false in the widget test if practical.
</execution_context>

<context>
@.planning/phases/09-settings-backup/09-RESEARCH.md
@lib/features/settings/presentation/tracking_diagnostics_screen.dart
@lib/features/matching/data/overpass_way_candidate_source.dart
@lib/features/matching/data/matching_providers.dart
@lib/core/db/daos/pending_road_fetches_dao.dart

`PendingRoadFetchesDao.listPending()` gives queue depth via `.length`; accessible via `ref.watch(appDatabaseProvider).pendingRoadFetchesDao`. `wayCandidateSourceProvider` builds the `OverpassWayCandidateSource`. The `kShowDiagnosticsHud` getter is added by Plan 09-03 in app_prefs.dart.
</context>

<tasks>

<task type="auto">
  <name>Task 1: OverpassWayCandidateSource cache hit/miss counters</name>
  <files>lib/features/matching/data/overpass_way_candidate_source.dart</files>
  <action>
Add two private int fields `int _cacheHits = 0; int _cacheMisses = 0;` with public getters `int get cacheHits => _cacheHits;` `int get cacheMisses => _cacheMisses;` and a `double? get cacheHitRate => (_cacheHits + _cacheMisses) == 0 ? null : _cacheHits / (_cacheHits + _cacheMisses);`.
In the tile-collection path (`_collectFreshTiles` / step 2-3 where each tile is classified as cache-satisfied vs missing): increment `_cacheHits` for each tile served from a fresh cache row, `_cacheMisses` for each tile that needs a network fetch. Keep it O(1) per tile; do not change fetch behavior. Add a docstring noting counters are per-instance and reset when the provider is rebuilt (e.g. after a restore invalidation).
  </action>
  <verify>`flutter analyze` clean.</verify>
  <done>Counters + getters + hitRate present; incremented on the hit/miss branches without altering fetch logic.</done>
</task>

<task type="auto">
  <name>Task 2: diagnosticsMetricsProvider</name>
  <files>lib/features/settings/data/diagnostics_metrics_provider.dart</files>
  <action>
A small value type + provider surfacing the two new metrics (keeps the domain-pure `TrackingDiagnostics` DTO untouched — it is built in TrackingService which has no DB/source access):
```dart
class DiagnosticsMetrics {
  const DiagnosticsMetrics({required this.matcherQueueDepth, required this.cacheHits, required this.cacheMisses, required this.cacheHitRate});
  final int matcherQueueDepth;
  final int cacheHits;
  final int cacheMisses;
  final double? cacheHitRate;
}
Future<DiagnosticsMetrics> readDiagnosticsMetrics(Ref ref) async {
  final depth = (await ref.read(appDatabaseProvider).pendingRoadFetchesDao.listPending()).length;
  final src = ref.read(wayCandidateSourceProvider);
  final hits = src is OverpassWayCandidateSource ? src.cacheHits : 0;
  final misses = src is OverpassWayCandidateSource ? src.cacheMisses : 0;
  ...
}
```
Expose it as a plain function the HUD calls on its polling tick (matches the HUD's existing `_refreshAsync` model), OR a `Provider<Future<DiagnosticsMetrics> Function()>`. Simplest: a top-level function `Future<DiagnosticsMetrics> readDiagnosticsMetrics(WidgetRef ref)` the HUD invokes. Package imports only; plain Provider if any.
  </action>
  <verify>`flutter analyze` clean.</verify>
  <done>Queue depth read from DAO; cache hits/misses read from the source (guarded by `is` check for the fixture impl).</done>
</task>

<task type="auto">
  <name>Task 3: Extend HUD + remove kDebugMode build guard</name>
  <files>lib/features/settings/presentation/tracking_diagnostics_screen.dart</files>
  <action>
1. In `_refreshAsync`, also fetch `DiagnosticsMetrics` (from Task 2) and store in state (guard `mounted`).
2. Add a new `_SectionHeader('Matcher / cache')` with rows: `matcherQueueDepth` (int), `cacheHits`, `cacheMisses`, `cacheHitRate` (format as `${(rate*100).toStringAsFixed(0)}%` or `—` when null).
3. Remove the `if (!kDebugMode) return const _ReleaseModeShortCircuit();` early-return in `build` (the screen is now reachable in release when toggled on). You MAY delete `_ReleaseModeShortCircuit` if now unused, or leave it dead-but-harmless — prefer deleting to keep analyze clean (unused_element). Keep the `foundation.dart` import only if still referenced.
Do NOT edit settings_screen.dart or the router here — the tile/route un-gating is Plan 09-07's job (serial tail owns settings_screen.dart + app_router.dart).
  </action>
  <verify>`flutter analyze` clean (watch for unused `kDebugMode`/`_ReleaseModeShortCircuit`).</verify>
  <done>HUD shows queue depth + cache metrics; release-mode short-circuit removed; no unused-element warnings.</done>
</task>

<task type="auto">
  <name>Task 4: Cache counter unit test</name>
  <files>test/features/matching/data/overpass_cache_counter_test.dart</files>
  <action>
Unit-test the counters on `OverpassWayCandidateSource`. Reuse the existing test scaffolding for this source (grep `test/features/matching/` for the existing overpass_way_candidate_source test + its cache/client fakes). Seed the cache DAO with fresh rows for some tiles, leave others missing (stub the client). Call `fetchWaysInBbox` over a bbox spanning both, assert `cacheHits > 0`, `cacheMisses > 0`, and `cacheHitRate` in (0,1). Also assert a fresh instance reports `cacheHitRate == null` before any call.
  </action>
  <verify>`flutter test test/features/matching/data/overpass_cache_counter_test.dart` green. `flutter analyze` clean.</verify>
  <done>Counters proven to increment on both branches; null-rate-before-first-call verified.</done>
</task>

</tasks>

<verification>
- `flutter analyze` clean.
- Counter test green.
- HUD reachable logic no longer depends on kDebugMode (the toggle-gated tile + route land in 09-07).
- Maps to SC5 (fix rate [existing] + matcher queue depth + cache-hit rate).
</verification>

<success_criteria>
- Cache hit/miss counters live on the source with a documented main-isolate-only limitation.
- HUD shows queue depth + cache-hit rate.
- HUD builds + runs in release (gate removed).
</success_criteria>

<output>
After completion, create `.planning/phases/09-settings-backup/09-06-SUMMARY.md`.
</output>
