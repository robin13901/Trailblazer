---
phase: 09-settings-backup
plan: 05
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - lib/features/settings/presentation/widgets/data_backup_section.dart
  - test/features/settings/presentation/data_backup_section_test.dart
autonomous: true

must_haves:
  truths:
    - "User taps Export and the backup file is created then handed to the OS share sheet"
    - "User taps Restore, picks a .trailblazer file, confirms a destructive warning, and the app swaps in the backup"
    - "A failed/invalid restore surfaces a clear error and does NOT leave the app in a broken state"
    - "Restore shows a progress/restoring state and refreshes app state after the swap (provider rebuild)"
  artifacts:
    - path: "lib/features/settings/presentation/widgets/data_backup_section.dart"
      provides: "Export + Restore tiles wiring BackupService + FilePlatform with confirm dialog + progress + result feedback"
      min_lines: 90
    - path: "test/features/settings/presentation/data_backup_section_test.dart"
      provides: "widget tests over FakeBackupService + FakeFilePlatform (export path, restore-confirm path, invalid-restore path)"
  key_links:
    - from: "data_backup_section.dart"
      to: "BackupService.createBackup + FilePlatform.shareFile"
      via: "export button"
      pattern: "createBackup"
    - from: "data_backup_section.dart"
      to: "FilePlatform.pickBackupFile + BackupService.restore"
      via: "restore button -> pick -> confirm -> restore"
      pattern: "restore\\("
---

<objective>
Deliver the SET-07/SET-08 UI: a Data & Backup section with an Export flow (create backup -> OS share sheet) and a Restore flow (pick file -> destructive-confirm dialog -> progress -> restore -> app-state refresh -> success/failure feedback), wiring the Plan 09-01 engine and Plan 09-02 file platform.

Purpose: Satisfies SC1 (export to picked destination) + SC2 (restore validates + swaps in place), user-visible.
Output: `DataBackupSection` widget + widget tests using the two Fakes.
</objective>

<execution_context>
Ralph loop: `flutter analyze` after every change; run the widget test in-loop (flows are behavior-sensitive).
</execution_context>

<context>
@.planning/phases/09-settings-backup/09-CONTEXT.md
@.planning/phases/09-settings-backup/09-RESEARCH.md
@lib/features/settings/presentation/widgets/data_management_section.dart

Depends on 09-01: `backupServiceProvider` / `BackupService` / `FakeBackupService`.
Depends on 09-02: `filePlatformProvider` / `FilePlatform` / `FakeFilePlatform`.
Reuse the confirm-dialog idiom from `DataManagementSection._onTapRefresh` (showDialog<bool> AlertDialog). Restore-invalidation timing (Pitfall 7): after `restore()` returns Ok, the appDatabaseProvider rebuild is scheduled for the next frame — show a "Restoring…" state and let dependents rebuild; do NOT read the DB synchronously.
</context>

<tasks>

<task type="auto">
  <name>Task 1: DataBackupSection — Export + Restore flows</name>
  <files>lib/features/settings/presentation/widgets/data_backup_section.dart</files>
  <action>
`DataBackupSection extends ConsumerStatefulWidget`. Two ListTiles under the section.

**Export tile** ("Back up my data", subtitle "Creates a shareable copy of all your trips & coverage"):
1. Set a local `_exporting = true` (spinner in trailing).
2. `final r = await ref.read(backupServiceProvider).createBackup();`
3. On `Ok(path)`: `await ref.read(filePlatformProvider).shareFile(path, subject: 'Trailblazer Backup');` then SnackBar "Backup ready to share" (or silent if share sheet handles it). On `Err`: SnackBar with the error message.
4. `finally` clear `_exporting` (guard `mounted`).

**Restore tile** ("Restore from backup", subtitle "Replaces ALL current data with a backup file"):
1. `final path = await ref.read(filePlatformProvider).pickBackupFile();` if null (cancelled) return.
2. Destructive confirm dialog (showDialog<bool>): title "Replace all data?", body "Restoring will PERMANENTLY replace all current trips, coverage, and settings with the contents of this backup. This cannot be undone." Cancel / "Replace" (use a warning-styled FilledButton). If not confirmed, return.
3. Set `_restoring = true` and show a blocking progress indicator (dialog or inline "Restoring…"). 
4. `final r = await ref.read(backupServiceProvider).restore(path);`
5. On `Ok`: SnackBar "Backup restored". The appDatabaseProvider is invalidated inside restore(); dependents rebuild next frame. Optionally pop back to the map so the tree re-reads fresh data. On `Err`: SnackBar "Restore failed: <message>" — the engine's safety-snapshot + validate-first guarantees the live DB is intact on validation failure.
6. `finally` clear `_restoring` (guard `mounted`).

All `context`/`ref` use after await guarded by `mounted`. Capture `ScaffoldMessenger.of(context)` before awaits (see DataManagementSection pattern). Use `withValues(alpha:)` for any alpha.
  </action>
  <verify>`flutter analyze` clean.</verify>
  <done>Export creates + shares; Restore picks -> confirms -> restores with progress + result feedback; cancellation paths return cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Widget tests (export, restore-confirm, invalid-restore)</name>
  <files>test/features/settings/presentation/data_backup_section_test.dart</files>
  <action>
Pump `DataBackupSection` in `ProviderScope(overrides: [backupServiceProvider.overrideWithValue(fakeBackup), filePlatformProvider.overrideWithValue(fakeFile)])` + MaterialApp with a Scaffold (for SnackBar/Dialog).
Cases:
1. **Export happy path:** tap Export, pumpAndSettle; assert `fakeBackup.createBackup` produced a path and `fakeFile.sharedPaths` contains it.
2. **Restore confirm path:** set `fakeFile.pickResult = '/fake/backup.trailblazer'`; tap Restore -> confirm dialog appears -> tap "Replace" -> assert `fakeBackup.restoredPaths` contains the picked path + "restored" SnackBar.
3. **Restore cancel:** tap Restore -> tap "Cancel" -> assert restore NOT called.
4. **Restore failure:** set `fakeBackup.restoreShouldFail = true`; confirm -> assert "Restore failed" SnackBar and no crash.
5. **Pick cancelled:** `fakeFile.pickResult = null`; tap Restore -> no dialog, restore not called.
  </action>
  <verify>`flutter test test/features/settings/presentation/data_backup_section_test.dart` green. `flutter analyze` clean.</verify>
  <done>All five flow paths asserted with the Fakes; no platform channel touched.</done>
</task>

</tasks>

<verification>
- `flutter analyze` clean.
- Widget tests green.
- Maps to SC1 (export -> share) + SC2 (restore validate+swap, user-facing).

## Deferred on-device checklist (batched to next drive / phase close-out)
These genuinely need a real device (share sheet + SAF/UIActivityViewController + file picker are platform-channel UI):
- [ ] Export produces a .trailblazer file and the iOS share sheet / Android ACTION_SEND offers iCloud Drive / Drive / Files.
- [ ] Restore picks that file back via the OS picker and the app shows restored trips/coverage after rebuild.
- [ ] Round-trip on a real device: export, wipe (or fresh install), restore, verify trips + coverage return.
- [ ] iOS document picker shows .trailblazer files (Info.plist doc-in-place from 09-02).
</verification>

<success_criteria>
- Export: create backup -> OS share sheet.
- Restore: pick -> destructive confirm -> progress -> restore -> refresh + feedback.
- Invalid/failed restore surfaces a clear error, app stays usable.
</success_criteria>

<output>
After completion, create `.planning/phases/09-settings-backup/09-05-SUMMARY.md`.
</output>
