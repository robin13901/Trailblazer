---
phase: 10-coverage-recompute-region-totals
plan: 04
type: execute
wave: 2
depends_on: ["10-03"]
files_modified:
  - lib/features/regions/data/region_totals_lookup.dart
  - lib/features/regions/data/coverage_compute_service.dart
  - lib/features/regions/data/coverage_compute_providers.dart
  - lib/features/regions/data/region_total_length_service.dart
  - lib/features/regions/domain/region_tiling.dart
  - lib/features/regions/domain/region_coverage.dart
  - lib/features/regions/presentation/providers/region_browser_provider.dart
  - lib/features/regions/presentation/widgets/region_card.dart
  - lib/features/regions/presentation/widgets/region_detail_sheet.dart
  - lib/app.dart
  - pubspec.yaml
autonomous: true

must_haves:
  truths:
    - "Per-region total road length comes from the bundled region_totals.json.gz table keyed by osm_id — zero runtime Overpass calls for totals"
    - "coverage_cache.real_total_length_m is populated from the bundled table during recompute"
    - "There is NO per-region spinner/pending/progress UI anywhere — a region shows its total-km or nothing"
    - "RegionTotalLengthService, region_tiling, and the real_total_progress_json accumulator are removed from the runtime path"
    - "Totals are correct at all levels including Bundesländer (no OOM, no throttling)"
  artifacts:
    - path: "lib/features/regions/data/region_totals_lookup.dart"
      provides: "Loads region_totals.json.gz once off the main isolate; O(1) osm_id→meters lookup"
      contains: "region_totals"
    - path: "lib/features/regions/data/coverage_compute_service.dart"
      provides: "recompute() populates real_total_length_m from RegionTotalsLookup"
      contains: "recompute"
  key_links:
    - from: "coverage_compute_service.dart recompute()"
      to: "RegionTotalsLookup (bundled table)"
      via: "lookup by region_id (osm_id string) → real_total_length_m"
      pattern: "region_totals|RegionTotalsLookup|realTotalLength"
    - from: "region_browser_provider.dart"
      to: "coverage_cache real_total_length_m"
      via: "totals from bundle, no pending/progress fields"
      pattern: "realTotalLengthM"
---

<objective>
Wire the bundled totals table (from 10-03) into `coverage_cache` population and DELETE the runtime Overpass totals path entirely (decision 8). The runtime tiler was ~30× too chatty and OOMed on Bundesländer; the bundled table replaces it — instant, complete, zero API calls, no spinner.

Purpose: SC3 — per-region totals served from the bundled table, no runtime Overpass, no per-region spinner ever.
Output: A `RegionTotalsLookup` loader, `recompute()` populating `real_total_length_m` from it, and the removal of `RegionTotalLengthService` + tiling + progress/spinner UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-coverage-recompute-region-totals/10-CONTEXT.md
@.planning/phases/10-coverage-recompute-region-totals/10-03-SUMMARY.md

# The load posture to mirror (read bytes on main isolate, parse in compute()):
@lib/features/admin/data/admin_region_lookup.dart
@lib/features/regions/data/coverage_compute_service.dart
@lib/features/regions/data/coverage_compute_providers.dart
@lib/features/regions/presentation/providers/region_browser_provider.dart
@lib/features/regions/data/region_total_length_service.dart
@lib/features/regions/domain/region_coverage.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: RegionTotalsLookup — load the bundled totals table + populate coverage_cache</name>
  <files>lib/features/regions/data/region_totals_lookup.dart, lib/features/regions/data/coverage_compute_service.dart, lib/features/regions/data/coverage_compute_providers.dart, pubspec.yaml</files>
  <action>
  Create `RegionTotalsLookup` mirroring `AdminRegionLookup`'s load posture EXACTLY: read `assets/admin/region_totals.json.gz` bytes on the main isolate via `rootBundle.load`, then parse in `compute()` off-isolate — `gzip.decode` → `utf8.decode` → `jsonDecode` → `Map<String,double>` (keys stay String to match `coverage_cache.region_id`). Expose `double? totalFor(String osmId)` (O(1)) and lazy one-time load (cache the future). Provider: plain `Provider<RegionTotalsLookup>` (codegen off).

  Register the new asset in `pubspec.yaml` under `flutter: assets:` (it's under `assets/admin/` which is likely already declared as a directory — verify; if the dir is declared, no change needed, but confirm the `.json.gz` is bundled).

  In `coverage_compute_service.dart`: inject `RegionTotalsLookup`. In `recompute()`, when upserting each region row, set `real_total_length_m` = `totalsLookup.totalFor(regionId)` (the bundled value). The bundled total is authoritative for `real_total_length_m`; keep the existing haversine `total_length_m` (fetched-ways total) as-is for backward compat, or set it equal — but the DISPLAYED denominator must be the bundled real total (fixes the Bayern==Miltenberg denominator). Update `coverage_compute_providers.dart` to pass the new lookup and REMOVE the `regionTotalLengthServiceProvider` wiring.
  </action>
  <verify>`flutter analyze` clean. `flutter test` — coverage_compute_service touches lib/core-adjacent data + has existing tests; add/adjust a test asserting `real_total_length_m` is populated from a fake RegionTotalsLookup. Grep confirms no runtime code path calls Overpass for totals.</verify>
  <done>recompute() writes bundled real totals into coverage_cache; RegionTotalsLookup loads off-isolate; no Overpass totals calls remain.</done>
</task>

<task type="auto">
  <name>Task 2: Delete RegionTotalLengthService + tiling + spinner/progress UI (decision 8)</name>
  <files>lib/features/regions/data/region_total_length_service.dart, lib/features/regions/domain/region_tiling.dart, lib/features/regions/domain/region_coverage.dart, lib/features/regions/presentation/providers/region_browser_provider.dart, lib/features/regions/presentation/widgets/region_card.dart, lib/features/regions/presentation/widgets/region_detail_sheet.dart, lib/app.dart</files>
  <action>
  Remove the runtime Overpass totals path fully:
  - DELETE `region_total_length_service.dart` and `region_tiling.dart` (and their tests). Remove `regionTotalLengthServiceProvider` from `coverage_compute_providers.dart`.
  - In `app.dart`: remove the startup call to `regionTotalLengthServiceProvider ... computeMissingTotals()` (~line 177) and any related scheduling.
  - In `region_coverage.dart`: remove the `totalPending`, `progressCellsDone`, `progressCellsPlanned` fields (or, if removing the fields ripples widely, hard-set them gone at the source — but prefer clean removal). A region now has a total-km or it doesn't; no pending state.
  - In `region_browser_provider.dart`: stop deriving `pending`/`progressCells*` from `realTotalProgressJson`; take `real_total_length_m` directly (null → show "—", never a spinner).
  - In `region_card.dart` / `region_detail_sheet.dart`: remove the "N/M Kacheln" progress / spinner UI; render total-km when present, "—" (or nothing) when absent. NO loading state.
  - `real_total_progress_json` column: leave the DB column in place (schema bumps are expensive — 10-RESEARCH State-of-the-Art) but stop reading/writing it from the runtime path. NO schema change (reuse `real_total_length_m`; `drift_backup_service.dart kCurrentSchemaVersion` stays untouched — confirm no schema bump so the recurring backup-version trap is avoided).
  </action>
  <verify>`flutter analyze` clean (dangling imports/refs to the deleted service/tiling resolved). `flutter test` green. Grep confirms zero references to `RegionTotalLengthService`, `region_tiling`, `totalPending`, `progressCellsDone`, `Kacheln` in `lib/`. Confirm `kCurrentSchemaVersion` unchanged (no schema bump).</verify>
  <done>Runtime totals service + tiling + spinner UI are gone; totals come only from the bundle; no schema bump.</done>
</task>

</tasks>

<verification>
- `flutter analyze` clean; `flutter test` green (this touches core data + region presentation — behaviour-sensitive, run tests in-loop).
- No runtime Overpass totals calls; no per-region spinner anywhere.
- No Drift schema change → `kCurrentSchemaVersion` in `drift_backup_service.dart` unchanged (verify explicitly — repeated past trap).
</verification>

<success_criteria>
- SC3: per-region totals served from the bundled table, zero runtime Overpass, instant, correct at all levels incl. Bundesländer, no spinner.
</success_criteria>

<output>
After completion, create `.planning/phases/10-coverage-recompute-region-totals/10-04-SUMMARY.md`. Confirm no schema bump and list every deleted/emptied symbol.
</output>
