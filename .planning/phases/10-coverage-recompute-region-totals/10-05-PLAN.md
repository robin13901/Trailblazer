---
phase: 10-coverage-recompute-region-totals
plan: 05
type: execute
wave: 3
depends_on: ["10-04"]
files_modified:
  - lib/features/regions/presentation/regions_screen.dart
  - lib/features/regions/presentation/widgets/recalculate_button.dart
  - lib/features/regions/data/recalculate_coverage_action.dart
  - lib/features/matching/data/trip_match_coordinator.dart
  - lib/features/regions/data/coverage_compute_service.dart
autonomous: true

must_haves:
  truths:
    - "A 'Regionen neu berechnen' action sits at the top of the Regions tab behind a confirmation dialog"
    - "Tapping it runs rematchAllStoredTrips() → recompute() → populate bundled totals over ALL stored trips without deleting any trip"
    - "After it completes, every region the trips pass through appears (Bayern, Landkreis Miltenberg, Miltenberg-town, Kleinheubach) with correct driven km"
    - "Recompute ALSO fires automatically (recompute-only, not full re-match) when a new trip's intervals land"
    - "The button shows progress while running and the app stays responsive"
    - "A matcher-performance investigation is done: clear wins landed, deferrals documented"
  artifacts:
    - path: "lib/features/regions/presentation/widgets/recalculate_button.dart"
      provides: "Confirmation-gated 'Regionen neu berechnen' button at Regions-tab top"
      contains: "neu berechnen"
    - path: "lib/features/regions/data/recalculate_coverage_action.dart"
      provides: "Orchestrates rematchAllStoredTrips → recompute → totals; progress state"
    - path: "lib/features/matching/data/trip_match_coordinator.dart"
      provides: "Auto recompute-only hook after intervals land"
      contains: "recompute"
  key_links:
    - from: "recalculate_button.dart"
      to: "RecalculateCoverageAction (rematch + recompute)"
      via: "confirmation dialog → run"
      pattern: "neu berechnen|rematchAllStoredTrips"
    - from: "trip_match_coordinator.dart after _writeIntervals"
      to: "CoverageComputeService.recompute()"
      via: "auto recompute-only trigger"
      pattern: "recompute"
---

<objective>
The primary user-facing deliverable + the auto seam + perf work.

- **Recalculate button (decision 4):** a "Regionen neu berechnen" action at the TOP of the Regions tab, behind a confirmation dialog (heavy op). Runs a FULL re-match + recompute over ALL stored trips (no deletion — decision 5): `rematchAllStoredTrips()` → `recompute()` → populate bundled totals. Fixes F1 (recompute is never re-triggered in the manual-only app, so driven-km is frozen and coarse regions never appear).
- **Auto-recompute seam (decision 6):** recompute ALSO fires automatically when a new trip's intervals land — the button is a fallback, not the only path. **Auto = recompute-only** (bounded, cache-first, cheap — rebuilds region rows from existing intervals), NOT a full re-match. Clean seam: `TripMatchCoordinator` after `_writeIntervals` / `transitionToMatched`.
- **OQ1-PERF (decision 6 add-on):** investigate matcher/recompute performance THIS phase; land clear wins, document deferrals. Don't gold-plate.

Purpose: SC1 — the Regions tab becomes self-serviceable and correct without a fresh drive.
Output: The button + confirmation + progress, the auto recompute-only hook, and a perf pass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-coverage-recompute-region-totals/10-CONTEXT.md
@.planning/phases/10-coverage-recompute-region-totals/10-RESEARCH.md
@.planning/phases/10-coverage-recompute-region-totals/10-04-SUMMARY.md

@lib/features/regions/presentation/regions_screen.dart
@lib/features/matching/data/trip_match_coordinator.dart
@lib/features/regions/data/coverage_compute_service.dart
# Reuse an existing confirm-dialog pattern:
@lib/features/settings/presentation/widgets/data_management_section.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recalculate action + confirmation-gated button at Regions-tab top</name>
  <files>lib/features/regions/data/recalculate_coverage_action.dart, lib/features/regions/presentation/widgets/recalculate_button.dart, lib/features/regions/presentation/regions_screen.dart</files>
  <action>
  `recalculate_coverage_action.dart`: an orchestrator (plain class + `Provider`) exposing `Future<Result<int>> run()` that: (1) `TripMatchCoordinator.rematchAllStoredTrips()` (re-runs the matcher over every stored trip — does NOT delete trips), (2) `CoverageComputeService.recompute()` (rebuilds coverage_cache region rows), (3) totals are populated inside recompute() from the bundled table (10-04) — no extra network step. Expose a progress signal (e.g. a `ValueNotifier`/state enum: idle → rematching(N/M) → recomputing → done/error) so the UI can show progress. Never throws (DomainError boundary → Result).

  `recalculate_button.dart`: a Consumer widget rendering a Liquid-Glass-consistent button labelled "Regionen neu berechnen" (German). On tap show a confirmation `AlertDialog` (reuse the `data_management_section.dart` confirm pattern) explaining it re-matches all trips and may take a while; on confirm, call `action.run()` and show progress (spinner + "N/M Trips" or a linear indicator) while running; on completion show a brief success/snackbar. Disable the button while running.

  `regions_screen.dart`: mount the button at the TOP of the tab (above the search field, inside the existing `Column` — add it as the first child, keep the `_SearchField` + `Divider` + `Expanded(_BrowserBody)` below). German copy throughout.
  </action>
  <verify>`flutter analyze` clean. `flutter test` — add a widget test that taps the button, confirms the dialog, and asserts the action is invoked (fake action). Grep: `regions_screen.dart` renders `RecalculateButton` as the first Column child.</verify>
  <done>Regions tab has a confirmation-gated "Regionen neu berechnen" button that runs full rematch+recompute with visible progress, no trip deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Auto recompute-only hook in TripMatchCoordinator (decision 6)</name>
  <files>lib/features/matching/data/trip_match_coordinator.dart, lib/features/regions/data/coverage_compute_service.dart</files>
  <action>
  Wire an AUTOMATIC recompute-only trigger: after a trip's intervals are written (`_writeIntervals` / `transitionToMatched` path in `TripMatchCoordinator`), fire `CoverageComputeService.recompute()` (recompute-only — rebuild region rows from existing intervals; NOT a re-match). Use `unawaited()` fire-and-forget consistent with the existing Phase-8 post-confirm hook pattern, wrapped so a failure logs and never breaks matching. Guard against re-entrancy / redundant runs if multiple intervals land close together (a simple in-flight bool or debounce is fine — don't over-engineer).

  Inject the `CoverageComputeService` (or its provider-read) into the coordinator following the existing DI style; if the coordinator can't easily reach it, add a callback seam the coordinator invokes and wire it at provider construction. Keep the heavy `rematchAllStoredTrips` path exclusively on the button (Task 1) — auto is recompute-only.
  </action>
  <verify>`flutter analyze` clean. `flutter test` — coordinator is in lib/features/matching (behaviour-sensitive); add/adjust a test asserting recompute is invoked after intervals land and that it is recompute-only (not rematch). No re-entrant storm on rapid interval writes.</verify>
  <done>New intervals landing auto-trigger a bounded recompute-only; the button remains the only full-rematch path.</done>
</task>

<task type="auto">
  <name>Task 3: OQ1-PERF investigation — land clear wins, document deferrals</name>
  <files>lib/features/regions/data/coverage_compute_service.dart, lib/features/matching/data/trip_match_coordinator.dart</files>
  <action>
  Investigate the hot paths per 10-RESEARCH RQ7 and land whatever is a CLEAR win; document the rest. Scope guardrails (do NOT gold-plate — goal is "recompute after a drive isn't annoying" and "the button finishes in reasonable time"):
  - **Incremental recompute (auto path):** the auto trigger fires often and today `recompute()` does deleteAll + upsert-all over the union bbox of ALL trips. For the auto (recompute-only, no-deletion) case, a targeted upsert touching only the regions the NEW trip's bbox intersects is feasible and low-risk (reuse `CoverageInvalidator`'s 5-point bbox probe to find touched regions). Implement this if it lands cleanly; keep full deleteAll+upsert for the BUTTON path (correctness guaranteed). If the targeted path proves risky, keep full recompute for auto too and DOCUMENT why it was deferred.
  - **Matcher throughput (button path):** confirm the `MatcherIsolate` is already kept warm across `rematchAllStoredTrips` (10-RESEARCH says it is — `_isolate.start()` idempotent) and that overlapping-bbox trips hit the Overpass tile cache. Do NOT invest in warm-R-Tree cross-trip reuse (needs significant isolate API changes for minimal gain at current trip counts) — document as deferred.
  Record findings + what shipped vs deferred in the SUMMARY.
  </action>
  <verify>`flutter analyze` clean; `flutter test` green (correctness of recompute unchanged for the button path — verify with existing recompute tests; if incremental auto-path lands, add a test that touched-region upsert equals a full recompute for a single-trip scenario).</verify>
  <done>Clear perf wins landed (at minimum: incremental auto-recompute OR a documented decision not to); warm-isolate reuse deferral documented. No gold-plating.</done>
</task>

</tasks>

<verification>
- `flutter analyze` clean; `flutter test` green (matching + regions data are behaviour-sensitive — run tests in-loop).
- Button runs full rematch+recompute over all trips without deleting any; auto path is recompute-only.
- On-device confirm (tap button → Bayern/Landkreis Miltenberg/Miltenberg-town/Kleinheubach appear with correct km) is a device check — defer to next drive per project convention; note in SUMMARY.
</verification>

<success_criteria>
- SC1: "Regionen neu berechnen" (confirmation-gated, Regions-tab top) runs full re-match + recompute over ALL stored trips (no deletion) and repopulates coverage_cache; every traversed region appears with correct driven km.
- Decision 6: auto recompute-only fires when intervals land; button is the fallback full path.
- OQ1-PERF has a home: wins landed, deferrals documented.
</success_criteria>

<output>
After completion, create `.planning/phases/10-coverage-recompute-region-totals/10-05-SUMMARY.md`. Include the OQ1-PERF findings (shipped vs deferred) and the deferred on-device confirm.
</output>
