---
phase: 10-coverage-recompute-region-totals
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/map/presentation/widgets/live_puck_bridge.dart
  - lib/features/map/presentation/providers/live_puck_applier.dart
  - lib/features/map/presentation/widgets/map_widget.dart
  - lib/features/map/presentation/widgets/map_screen.dart
autonomous: true

must_haves:
  truths:
    - "During an active recording the puck sits at the tip of the live coverage line and moves in the same tick the line extends"
    - "The MapLibre native location dot is suppressed while recording and restored when idle"
    - "The non-recording map is unchanged — native puck still shown when not recording"
  artifacts:
    - path: "lib/features/map/presentation/widgets/live_puck_bridge.dart"
      provides: "Headless bridge drawing our own puck from liveFixProvider (same feed as the line)"
      contains: "liveFixProvider"
    - path: "lib/features/map/presentation/providers/live_puck_applier.dart"
      provides: "MapLibre symbol/circle layer applier for the live puck"
    - path: "lib/features/map/presentation/widgets/map_widget.dart"
      provides: "Native puck suppressed while trackingStateProvider is active"
      contains: "myLocationEnabled"
  key_links:
    - from: "live_puck_bridge.dart"
      to: "liveFixProvider (LiveFixSample)"
      via: "ref.listen — same feed the trail consumes, updated in the same tick"
      pattern: "liveFixProvider"
    - from: "map_widget.dart myLocationEnabled"
      to: "trackingStateProvider"
      via: "suppress native dot while recording"
      pattern: "trackingStateProvider|myLocationEnabled"
---

<objective>
Fix F5: while recording, the blue location puck lags the live coverage line and jumps forward late. This is a rendering-source mismatch — the line is drawn by `LiveTrailBridge._onFix` from `liveFixProvider` (updates in-tick), while the puck is MapLibre's NATIVE location layer on its own slower, smoothed cadence.

Fix (clean, low-risk, reuses the existing seam): draw OUR OWN puck as a MapLibre symbol/circle layer from the SAME `LiveFixSample` feed the line consumes, updated in a sibling bridge so it lands at the newest trail point every tick. Suppress the native dot DURING recording; restore it when idle. Keep the native puck for the non-recording map.

Purpose: The puck and the coverage line move together — no visible lag-then-jump (SC6).
Output: A live-puck bridge + applier, native-dot suppression wired to `trackingStateProvider`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-coverage-recompute-region-totals/10-CONTEXT.md

# The line bridge + applier to mirror exactly (same lifecycle, same style-tick handling):
@lib/features/map/presentation/widgets/live_trail_bridge.dart
@lib/features/map/presentation/providers/live_trail_applier.dart
@lib/features/map/presentation/widgets/map_widget.dart
@lib/features/trips/domain/live_fix_sample.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-puck applier + bridge from the liveFix feed</name>
  <files>lib/features/map/presentation/providers/live_puck_applier.dart, lib/features/map/presentation/widgets/live_puck_bridge.dart</files>
  <action>
  Model BOTH files on `LiveTrailApplier` / `LiveTrailBridge` (read them first — copy their controller-null guards, `unawaited()` dispatch, and `mapStyleLoadedTickProvider` re-add-on-style-reload handling verbatim; the 06-05 "map must never crash" invariants apply).

  `live_puck_applier.dart` — a MapLibre layer applier that adds a single-point GeoJSON source + a circle (or symbol) layer for the puck. API mirroring LiveTrailApplier: `addOrUpdate(LatLng point, {double? heading})`, `remove()`. Draw a filled circle with a contrasting stroke so it reads as a location puck; if `LiveFixSample.heading` is non-null, rotate a directional symbol (heading is already available). Place the layer ABOVE the live trail layer so the puck sits on top of the line tip. Guard every controller call for null; dispatch async via `unawaited()` with caught throws.

  `live_puck_bridge.dart` — headless `ConsumerStatefulWidget` rendering `const SizedBox.shrink()`, mirroring LiveTrailBridge:
    - `ref.listen(liveFixProvider)`: on each accepted `LiveFixSample`, call `applier.addOrUpdate(LatLng(sample.lat, sample.lng), heading: sample.heading)` — SAME feed, SAME tick as the trail, so the puck lands at the newest trail point.
    - `ref.listen(trackingStateProvider)`: on transition to `TrackingIdle`, call `applier.remove()`.
    - `mapStyleLoadedTickProvider`: on tick change re-add source+layer from the last known point (style reload wipes programmatic sources — Pitfall 1).
  </action>
  <verify>`flutter analyze` clean. Bridge renders SizedBox.shrink; applier guards null controller. Run `flutter test` — these touch map presentation with existing sibling tests.</verify>
  <done>A puck point-layer is driven by liveFixProvider in the same tick as the trail line and removed on idle.</done>
</task>

<task type="auto">
  <name>Task 2: Suppress native dot while recording + mount the bridge</name>
  <files>lib/features/map/presentation/widgets/map_widget.dart, lib/features/map/presentation/widgets/map_screen.dart</files>
  <action>
  In `map_widget.dart`: today `myLocationEnabled: isGranted` and `myLocationRenderMode` is gated on `isGranted`. Add a watch of `trackingStateProvider` (via a Consumer or `ref.watch`) so that while recording (state != TrackingIdle) `myLocationEnabled` is `false` and `myLocationRenderMode` is `MyLocationRenderMode.normal`/none — suppressing the native dot. When idle, restore the current behaviour exactly (`myLocationEnabled: isGranted`, existing render-mode gating). Respect the MapLibreMap assert: compass render-mode requires `myLocationEnabled=true`, so when suppressing set render mode to the non-compass value. Keep TrackingCameraSync's heading-up behaviour intact (it already drives the camera; this only hides the native dot).

  In `map_screen.dart`: mount `LivePuckBridge` as a zero-size `Positioned` OUTSIDE any `if (isMapTab)` guard, right next to where `LiveTrailBridge` is mounted (same pattern), so it persists across tab switches.
  </action>
  <verify>`flutter analyze` clean. Grep: `map_widget.dart` references `trackingStateProvider`; `map_screen.dart` mounts `LivePuckBridge` beside `LiveTrailBridge`. Run `flutter test` for map_widget/map_screen tests; fix any that assert `myLocationEnabled` unconditionally by accounting for the recording state.</verify>
  <done>Native dot hidden during recording, restored when idle; our puck bridge is mounted and persistent.</done>
</task>

</tasks>

<verification>
- `flutter analyze` clean; `flutter test` green (map presentation has existing tests — this is behaviour-sensitive per the tiered Ralph Loop).
- On-device confirm (puck riding the line tip) is a visual check — defer to the next drive per the project's defer-in-car-verification convention; note it in the SUMMARY as a deferred device confirm.
</verification>

<success_criteria>
- SC6: during recording the live puck stays at the tip of the live coverage line (same liveFixSample feed; native puck suppressed while recording) — no lag-then-jump.
</success_criteria>

<output>
After completion, create `.planning/phases/10-coverage-recompute-region-totals/10-02-SUMMARY.md`. Record the deferred on-device visual confirm.
</output>
