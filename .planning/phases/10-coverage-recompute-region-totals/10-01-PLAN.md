---
phase: 10-coverage-recompute-region-totals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/regions/presentation/widgets/region_card.dart
  - lib/features/coverage/data/coverage_invalidator.dart
  - .planning/REQUIREMENTS.md
autonomous: true

must_haves:
  truths:
    - "Kleinheubach (L8) shows the badge 'Gemeinde / Stadt', not 'Landkreis'"
    - "Landkreis Miltenberg (L6) shows the badge 'Landkreis', not 'Regierungsbezirk'"
    - "After a trip is confirmed/discarded, L9 (Ortsteil) coverage rows are invalidated alongside 4/6/8/10"
    - "QUA-01, QUA-04, QUA-07 are formally de-scoped in REQUIREMENTS.md with a note tying them to the dropped Hardening phase"
  artifacts:
    - path: "lib/features/regions/presentation/widgets/region_card.dart"
      provides: "Corrected levelLabel() German region-type mapping (F3 fix)"
      contains: "levelLabel"
    - path: "lib/features/coverage/data/coverage_invalidator.dart"
      provides: "kCoverageAdminLevels including 9 (F2 fix)"
      contains: "kCoverageAdminLevels"
    - path: ".planning/REQUIREMENTS.md"
      provides: "QUA-01/04/07 marked de-scoped (decision 9)"
  key_links:
    - from: "region_card.dart levelLabel()"
      to: "German OSM admin hierarchy"
      via: "switch on admin_level"
      pattern: "levelLabel"
---

<objective>
Three small, independent, low-risk corrections that need no new architecture:
- **F3 (badge fix):** `region_card.dart levelLabel()` is shifted one level in the German OSM hierarchy. Correct it so L6=Landkreis and L8=Gemeinde/Stadt (Kleinheubach currently mislabelled "Landkreis" — the user's exact complaint).
- **F2 (invalidator alignment):** `CoverageInvalidator.kCoverageAdminLevels` is `[4,6,8,10]` — missing 9. `CoverageComputeService.kComputeAdminLevels` is already `[4,6,8,9,10]`. Align the invalidator so L9 Ortsteil rows are not left stale after a trip changes.
- **Decision 9 (docs de-scope):** Formally mark QUA-01/04/07 as de-scoped in REQUIREMENTS.md, tied to the dropped Hardening phase — a clean removal, not silent loss.

Purpose: Fixes the two most visible/cheap correctness bugs and closes the orphaned-requirements bookkeeping in one tidy plan.
Output: Corrected badge labels, aligned invalidation levels, de-scoped QUA entries.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-coverage-recompute-region-totals/10-CONTEXT.md

@lib/features/regions/presentation/widgets/region_card.dart
@lib/features/coverage/data/coverage_invalidator.dart
@lib/features/regions/data/coverage_compute_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix badge labels (F3) + align invalidation levels (F2)</name>
  <files>lib/features/regions/presentation/widgets/region_card.dart, lib/features/coverage/data/coverage_invalidator.dart</files>
  <action>
  In `region_card.dart`, rewrite the `levelLabel(int level)` switch to the correct German OSM hierarchy (Bavaria/most states — Regierungsbezirk L5 is not in scope):
    4 => 'Bundesland'
    6 => 'Landkreis'
    8 => 'Gemeinde / Stadt'
    9 => 'Ortsteil'
    10 => 'Ortsteil / Stadtteil'
  Keep the existing default/fallback arm for any unexpected level. Do NOT touch the widget layout — only the label mapping. Verify the current arms being replaced are: 4→Bundesland, 6→Regierungsbezirk, 8→Landkreis, 9→Gemeindeverband, 10→Gemeinde/Ortsteil.

  In `coverage_invalidator.dart`, change `const List<int> kCoverageAdminLevels = [4, 6, 8, 10];` to `[4, 6, 8, 9, 10];` so it matches `CoverageComputeService.kComputeAdminLevels`. Update the doc comment above it to note L9 (Ortsteil) is now included (F2 — 10-CONTEXT.md).
  </action>
  <verify>`flutter analyze` clean. Grep confirms `levelLabel` returns 'Landkreis' only for level 6 and 'Gemeinde / Stadt' for level 8; `kCoverageAdminLevels` is `[4, 6, 8, 9, 10]`.</verify>
  <done>Kleinheubach (L8) → "Gemeinde / Stadt"; Landkreis Miltenberg (L6) → "Landkreis"; L9 rows invalidated with the others.</done>
</task>

<task type="auto">
  <name>Task 2: De-scope QUA-01/04/07 in REQUIREMENTS.md (decision 9)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
  Mark QUA-01 (widget-test coverage), QUA-04 (patrol E2E), QUA-07 (real-device battery gauntlet) as **de-scoped**:
  - In the requirement checklist entries (the `- [ ] **QUA-0X**` lines ~167/170/173), append a bracketed note: `[DE-SCOPED 2026-07-17 — tied to the dropped Hardening phase; see ROADMAP + 10-CONTEXT decision 9]`. Do NOT delete the requirement text.
  - In the mapping table rows (~349/352/355) change the "Phase 10: Hardening" / "Pending" cells to `De-scoped (Hardening dropped 2026-07-17)`.
  - If REQUIREMENTS.md carries a coverage total/count that treats these as active v1 requirements, adjust the honest count wording to reflect the de-scope (do NOT silently drop the requirement IDs — they remain listed, just marked de-scoped). Keep it consistent with ROADMAP's 106/106 framing.
  </action>
  <verify>Grep for QUA-01/04/07 shows all three carry the de-scoped note in both the checklist and the mapping table. No other requirement rows changed.</verify>
  <done>QUA-01/04/07 are explicitly de-scoped with a Hardening-drop rationale; nothing else altered.</done>
</task>

</tasks>

<verification>
- `flutter analyze` clean (badge + invalidator are lib/ changes — run `flutter test` for any region_card / coverage_invalidator tests per the tiered Ralph Loop).
- If a `region_card` widget test asserts the old labels, update it to the corrected mapping.
- REQUIREMENTS.md de-scope is a docs-only edit — no code impact.
</verification>

<success_criteria>
- Region-type badges are correct (SC2): L4 Bundesland, L6 Landkreis, L8 Gemeinde/Stadt, L9 Ortsteil, L10 Ortsteil/Stadtteil.
- CoverageInvalidator invalidates L9 (supports SC1 correctness).
- QUA-01/04/07 formally de-scoped (decision 9 has a home).
</success_criteria>

<output>
After completion, create `.planning/phases/10-coverage-recompute-region-totals/10-01-SUMMARY.md`
</output>
